name: flutter_setup_runner
description: A Dart CLI tool to automate repetitive Flutter project setup tasks.
version: 1.0.0

environment:
  sdk: ^3.10.8

dependencies:
  args: ^2.4.2
  ansi_styles: ^0.3.2

dev_dependencies:
  lints: ^6.0.0
  test: ^1.25.6

executables:
  flutter_setup: